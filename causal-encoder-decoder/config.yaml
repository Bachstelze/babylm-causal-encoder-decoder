{
  # Interaction arguments
  "datapoint_length": 512,
  "dataset": "BabyLM-2026-Strict",
  "words_per_epoch": 100000000,
  "n_epochs": 10,
  "batch_size": 16,

  "learning_rate": 0.00005,
  "weight_decay": 0,
  "warmup_ratio": 0.01,
  "sft_learning_rate": 0.00005,
  "gradient_clip_norm": 1,
  "seed": -1,

  # Encoder-decoder model architecture parameters
  "from_pretrained": false,
  "model_name_or_path": null,
  "decoder_start_token_id": null,

  # Source/target split ratio (fraction of datapoint used as encoder source)
  "source_ratio": 0.5,

  # Encoder parameters (customizable per module)
  "encoder_n_layer": 6,
  "encoder_n_embd": 768,
  "encoder_n_head": 12,
  "encoder_n_inner": null,
  "encoder_activation_function": "gelu_new",

  # Decoder parameters (customizable per module)
  "decoder_n_layer": 6,
  "decoder_n_embd": 768,
  "decoder_n_head": 12,
  "decoder_n_inner": null,
  "decoder_activation_function": "gelu_new",

  # Experiment arguments
  "base_folder": "experiments",
  "experiment_name": "testing",

  "use_wandb": false,
  "wandb_experiment_name": "experiment_name",
  "wandb_project_name": "babylm_encdec",
}
